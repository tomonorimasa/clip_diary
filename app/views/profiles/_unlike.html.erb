<%= link_to like_path(current_user.likes.find_by(board_id: board.id)),
            id: "js-like-button-for-board-#{board.id}",
            class:"float-right",
            method: :delete,
            remote: true do %>
  <%= icon 'fas', 'star' %>
<% end %>